use super::Solve;

pub struct Day3 {
    pub day: u8,
    pub input: String
}

impl Solve for Day3 {
    fn solve_task_one(&self) -> Result<String, &'static str> {
        Ok(String::new())
    }
    
    fn solve_task_two(&self) -> Result<String, &'static str> {
        Ok(String::new())
    }
}
