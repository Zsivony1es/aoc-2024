use super::Solve;

pub struct Day2 {
    pub day: u8,
    pub input: String
}

impl Solve for Day2 {
    fn solve_task_one(&self) -> Result<String, &'static str> {
        Ok(String::new())
    }
    
    fn solve_task_two(&self) -> Result<String, &'static str> {
        Ok(String::new())
    }
}
